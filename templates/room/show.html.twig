{% extends 'base.html.twig' %}

{% block title %}Room{% endblock %}

{% block body %}
    <h1 class="titlecreate">Room</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ room.id }}</td>
            </tr>
            <tr>
                <th>NameRoom</th>
                <td>{{ room.nameRoom }}</td>
            </tr>
            <tr>
                <th>TypeRoom</th>
                <td>{{ room.typeRoom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('room_liste') }}">back to list</a>

    <a href="{{ path('room_edit', {'id': room.id}) }}">edit</a>

    {{ include('room/_delete_form.html.twig') }}
{% endblock %}
