{% extends 'base.html.twig' %}

{% block title %}Student{% endblock %}

{% block body %}
    <h1 class="titlecreate">Student</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ student.id }}</td>
            </tr>
            <tr>
                <th>Matricule</th>
                <td>{{ student.matricule }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ student.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ student.lastName }}</td>
            </tr>
            <tr>
                <th>Birthday</th>
                <td>{{ student.birthday }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ student.email }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ student.phone }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ student.adresse }}</td>
            </tr>
            <tr>
                <th>Sholarship</th>
                <td>{{ student.sholarship }}</td>
            </tr>
            <tr>
                <th>Housing</th>
                <td>{{ student.housing }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('student_liste') }}">back to list</a>

    <a href="{{ path('student_edit', {'id': student.id}) }}">edit</a>

    {{ include('student/_delete_form.html.twig') }}
{% endblock %}
